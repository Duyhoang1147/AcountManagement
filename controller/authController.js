const Account = require('../models/Account');

exports.createAccount = async (req, res) => {
    try {
        const account = new Account.cre
    } catch (error) {
        console.log(error);
    }
};